default files = "./";
default output = "file://" + files;

files + "zvdd_collections-test-set.xml" |
open("file") |
read("marcxml") |
morph(files + "../morph-zvdd_collection-rdfld.xml") |
encode("dot") |
write(output + "morph-zvdd_collection-rdfld.dot")
;